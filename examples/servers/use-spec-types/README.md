# Using the core specification types

To use the core specification's types, use either the `types`, `20250618`, etc. exports.

## 🏃 Build & run the server

```
npm i
npm run build
npm run start
```
